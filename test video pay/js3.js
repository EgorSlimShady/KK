const express = require('express');
const app = express();

// Обработчик GET-запроса для получения уникального кода
app.get('/getCode', (req, res) => {
    // Здесь нужно сгенерировать уникальный код и сохранить его на сервере
    // Затем отправить сгенерированный код в ответе на запрос
    
    const uniqueCode = generateUniqueCode(); // Функция для генерации уникального кода
    
    // Сохранить уникальный код на сервере
    // Обычно это делается с использованием базы данных или временного хранилища
    
    res.send(uniqueCode); // Отправить уникальный код в ответе на запрос
});

// Обработчик POST-запроса для проверки кода
app.post('/checkCode', (req, res) => {
    const receivedCode = req.body.code; // Получить код из запроса
    
    // Здесь нужно проверить, действителен ли полученный код
    // Если код действителен, вернуть ответ "Действителен"
    // Если код недействителен, вернуть ответ "Недействителен"
});

// Запуск сервера
app.listen(3000, () => {
    console.log('Сервер запущен на порту 3000');
});